services:
  - type: web
    name: medtechai-backend
    env: node
    plan: free
    buildCommand: cd src/backend && npm install
    startCommand: cd src/backend && node server-simple.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000